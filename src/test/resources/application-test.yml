#Configurações do Banco de Dados
spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/app_test?createDatabaseIfNotExist=true&serverTimeZone=UTC
    username: root
    password: root

    hikari:
      maximum-pool-size: 1

  flyway:
    locations: classpath:db/migration